# Ferry ランディングページ配信 Worker。
# ferry.kagayoi.com は R2 バケット ferry-updates のカスタムドメイン。
# 下のルートを被せると Worker が Custom Domain より優先され、worker.js が
# "/" のみランディングページを返し、それ以外のパスは R2 にそのまま委譲する。
name = "ferry-landing"
main = "worker.js"
compatibility_date = "2026-05-01"

routes = [
  { pattern = "ferry.kagayoi.com/*", zone_name = "kagayoi.com" },
  # 旧ドメイン（2027/05/31 の nephilim.jp 廃止まで併走。出荷済みバイナリの自動更新用）
  { pattern = "ferry.nephilim.jp/*", zone_name = "nephilim.jp" },
]

# index.html を文字列として worker.js に import する（Text モジュール）。
[[rules]]
type = "Text"
globs = ["**/*.html"]
