{
  "name": "ferry-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Ferry WebSocket リレー (Cloudflare Workers + Durable Objects, Hibernation 対応)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.0"
  }
}
